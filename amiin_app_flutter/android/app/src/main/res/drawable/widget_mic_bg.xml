<?xml version="1.0" encoding="utf-8"?>
<!-- Fond du bouton vocal — repris du chat : cercle turquoise doux + anneau fin.
     (au repos, comme l'état « mic_none » de la barre de saisie du chat). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@color/widget_voice_bg" />
    <stroke android:width="0.8dp" android:color="@color/widget_hair" />
</shape>
